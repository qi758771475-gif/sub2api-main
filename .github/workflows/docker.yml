name: Docker Build and Push

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - dockerfile: Dockerfile
            platforms: linux/amd64,linux/arm64,linux/arm/v7
            tag_suffix: ''
          - dockerfile: Dockerfile.ubuntu
            platforms: linux/amd64,linux/arm64,linux/arm/v7
            tag_suffix: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          VERSION=${TAG#v}
          MAJOR=${VERSION%.*}
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          echo "MAJOR=${MAJOR}" >> $GITHUB_OUTPUT

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./${{ matrix.dockerfile }}
          platforms: ${{ matrix.platforms }}
          push: true
          tags: |
            chenqi2/sub2api:${{ steps.version.outputs.VERSION }}${{ matrix.tag_suffix && format('-{0}', matrix.tag_suffix) || '' }}
            chenqi2/sub2api:${{ steps.version.outputs.MAJOR }}${{ matrix.tag_suffix && format('-{0}', matrix.tag_suffix) || '' }}
          build-args: |
            VERSION=${{ steps.version.outputs.VERSION }}
            COMMIT=${{ github.sha }}
            DATE=${{ github.event.head_commit.timestamp }}
            GOPROXY=https://goproxy.cn,direct
            GOSUMDB=sum.golang.google.cn